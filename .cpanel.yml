---
deployment:
  tasks:
    - export DEPLOY_PATH=/home/yourusername/public_html   # Change 'yourusername' to your actual cPanel username
    - /bin/cp -r . $DEPLOY_PATH                            # Copies everything from repo to public_html